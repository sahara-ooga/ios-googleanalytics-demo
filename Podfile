# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
pod ‘Google/Analytics’


target 'ios-googleanalytics-demo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-googleanalytics-demo

  target 'ios-googleanalytics-demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-googleanalytics-demoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
